Apri il file:
client/src/components/map/map-section.tsx

Obiettivo:
quando faccio DOPPIO CLICK su un task nella mappa, deve essere evidenziato il task corrispondente nella timeline.
Questo DEVE funzionare anche per i task nei "containers" (non assegnati) che usano marker Google nativi.

REQUISITO IMPORTANTE:
- La mappa deve CONTINUARE a zoommare con il doppio click (NON inserire disableDoubleClickZoom: true e NON disabilitare il double click zoom globalmente).

Cosa fare:

1) NON TOCCARE la logica dei marker HTML custom (quelli creati con un div) se già funziona.
   Non modificare quella sezione.

2) Correggi SOLO la parte dei marker Google nativi (new google.maps.Marker) usati per i task non assegnati / nei containers:
   - Implementa un vero handler di doppio click:
     marker.addListener('dblclick', ...)
   - Sul dblclick fai SOLO il toggle dell’evidenziazione nella timeline usando window.mapFilteredTaskId:
     const current = (window as any).mapFilteredTaskId;
     (window as any).mapFilteredTaskId = current === task.name ? null : task.name;

   - Il dblclick NON deve aprire il dettaglio task (non deve chiamare setSelectedTask(task)).
   - Il click singolo deve continuare ad aprire il dettaglio come prima (setSelectedTask(task)).
   - Se oggi usi un timer per distinguere click singolo/doppio click, mantienilo per il click singolo:
     • click singolo: setTimeout(...) -> setSelectedTask(task)
     • dblclick: clearTimeout se presente + toggle highlight
   - NON impedire lo zoom della mappa: quindi niente disableDoubleClickZoom e niente stopPropagation sulla mappa globale.
     (Se puoi, limita qualsiasi prevenzione solo all’apertura del dettaglio, non al comportamento della mappa.)

3) Non creare nuovi file.
4) Non modificare altri componenti.
5) Le modifiche devono essere solo in questo file.

Alla fine salva il file e assicurati che:
- dblclick su marker nativo (containers) evidenzia/disattiva evidenziazione in timeline
- click singolo continua ad aprire il dettaglio
- la mappa continua a zoommare con il doppio click
