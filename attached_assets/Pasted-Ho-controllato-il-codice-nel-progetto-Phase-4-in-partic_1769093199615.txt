Ho controllato il codice nel progetto (Phase 4 in particolare) e le modifiche che descrivi sono quasi tutte corrette, ma ci sono 2 punti importanti da sistemare per essere “a prova di bug” e aderenti al vincolo OT corta (≤2h).

✅ 1) Limite assoluto 5 task per cleaner (HARD)

Implementazione corretta.

In server/services/optimizer/phase4.ts:

hai export const ABSOLUTE_MAX_LOAD = 5;

è applicato in tryInsertTask (blocca prima di tutto)

è applicato anche in trySwapForTask con if (tasksForSim.length + 1 > ABSOLUTE_MAX_LOAD) continue;

trySingleAssignment passa da tryInsertTask, quindi è coperto.

👉 Nota: questo impedisce il caso “6 task” come quello del cleaner 771, indipendentemente da L2.

✅ 2) Vincoli hard OT e straordinaria (HARD)

Implementazione buona e coerente, con una correzione consigliata (vedi sotto).

In checkHardConstraints(schedule, task, tasksMap):

straordinaria: se schedule.canDoStraordinaria === false → reject ✅

OT lunga (≥360 min): se schedule non vuoto → reject ✅

se schedule ha già OT lunga → blocca qualunque altro task ✅

se schedule ha OT corta → blocca oltre 2 task totali, e impedisce extra >120 min ✅

⚠️ Correzione consigliata (importante)

La condizione:

if (task.straordinaria && schedule.canDoStraordinaria === false)


è fragile se in qualche punto canDoStraordinaria risultasse undefined.

✅ Meglio renderla fail-closed:

if (task.straordinaria && schedule.canDoStraordinaria !== true) {
  return { compatible: false, reason: 'CANNOT_DO_STRAORDINARIA' };
}


In runPhase4.ts tu già defaulti canDoStraordinaria a false, quindi oggi sei “coperto”, ma questa modifica ti protegge da futuri refactor o schedule creati altrove.

❌ 3) OT corta: manca un pezzo del vincolo “extra ≤ 2h” in un caso

Qui c’è un buco reale.

Caso: stai inserendo una OT corta su un cleaner che ha già 1 task non-OT.
Tu controlli solo il conteggio (existingNonOTs.length > 1) ma non controlli se quel task non-OT già presente è ≤ 2h.

Quindi oggi può succedere:

cleaner ha 1 task normale da 180 min

inserisci OT corta

passa i check hard, ma viola la regola “OT corta + 1 extra ≤ 2h”.

✅ Fix in checkHardConstraints, dentro:

if (taskDurationMin < 360 && schedule.tasks.length > 0) {


Aggiungi:

if (existingNonOTs.length === 1) {
  const extraDur = (existingNonOTs[0]?.cleaningTimeMinutes ?? 60);
  if (extraDur > 120) {
    return { compatible: false, reason: 'EXTRA_TASK_EXCEEDS_2H' };
  }
}


Questo rende il vincolo OT corta simmetrico:

“aggiungo extra dopo OT corta” → già controllato ✅

“aggiungo OT corta su extra già presente” → dopo fix ✅

✅ 4) Propagazione dati cleaner (role/contractType/canDoStraordinaria) nelle mutazioni

Corretto.

In runPhase4.ts carichi role, contractType, can_do_straordinaria da cleaners e li metti in CleanerSchedule ✅

In applyInsertion() preservi quei campi nel nuovo schedule ✅

In trySwapForTask quando costruisci newSchedule preservi i campi ✅

Quindi non si perdono durante insert/swap ✅

✅ 5) Checkout/checkin “non iniziare prima del checkout” / “non finire dopo checkin”

Anche se non lo hai citato nelle modifiche, è già garantito perché Phase 4 usa simulateSequence() di Phase 3.

In simulateSequence (Phase 3):

lo start viene fatto come max(arrival, checkoutTime) ✅

se end > checkinDateTime → fallisce con TIME_WINDOW_IMPOSSIBLE ✅

Phase 4 se !simResult.ok rifiuta sempre l’inserimento/swap ✅

Quindi questa regola è hard e non viene rilassata.

Nota sulla tua “Nota tecnica” (compatibilità role/contractType con typeApt)

Confermo: non puoi implementarla bene finché TaskForScheduling non contiene typeApt/apartmentType.
Quindi la tua nota è corretta.

Verdict finale

✅ Le modifiche su ABSOLUTE_MAX_LOAD = 5, inserite nei punti giusti, sono corrette e risolvono il caso “6 task”.
✅ La propagazione dei campi cleaner attraverso mutazioni è corretta.
⚠️ Sistemare il check di canDoStraordinaria per essere fail-closed.
❌ Fix necessario: OT corta deve verificare anche la durata dell’extra quando l’OT corta viene inserita su uno schedule che ha già 1 task non-OT.